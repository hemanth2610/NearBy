<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/space_12"
    android:background="@drawable/bg_glass_panel"
    android:elevation="@dimen/shadow_small"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:padding="@dimen/space_12">

    <ImageView
        android:id="@+id/iv_fav_thumbnail"
        android:layout_width="76dp"
        android:layout_height="76dp"
        android:background="@drawable/bg_glass_panel"
        android:contentDescription="Saved Place"
        android:scaleType="centerCrop"
        android:src="@drawable/bg_wave_top" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/space_12"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_fav_title"
            style="@style/Typography.HeadlineSmall"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/text_primary"
            tools:text="Qutub Minar" />

        <TextView
            android:id="@+id/tv_fav_category"
            style="@style/Typography.Caption"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textColor="@color/text_secondary"
            tools:text="Historical • Delhi" />

        <TextView
            android:id="@+id/tv_fav_rating"
            style="@style/Typography.Caption"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="@color/emerald_500"
            tools:text="★ 4.0" />
    </LinearLayout>

    <ImageButton
        android:id="@+id/btn_remove_fav"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:background="@drawable/bg_skip_pill"
        android:contentDescription="Remove"
        android:src="@drawable/ic_heart_filled"
        app:tint="@color/status_danger" />

</LinearLayout>
