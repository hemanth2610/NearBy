<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/place_info_card_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="@dimen/space_16"
    android:background="@drawable/bg_glass_panel"
    android:elevation="@dimen/shadow_large"
    android:orientation="vertical"
    android:padding="@dimen/space_16"
    android:visibility="gone">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <!-- Place Thumbnail Image -->
        <ImageView
            android:id="@+id/iv_place_thumbnail"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:background="@drawable/bg_glass_card"
            android:contentDescription="Place Thumbnail"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_app_logo" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/space_12"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tv_place_name"
                style="@style/Typography.HeadlineSmall"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="Paradise Beach Resort"
                android:textColor="@color/text_primary" />

            <TextView
                android:id="@+id/tv_place_category"
                style="@style/Typography.Caption"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/space_4"
                android:text="Beach &amp; Coastal Resort • 1.2 km"
                android:textColor="@color/emerald_500" />
        </LinearLayout>

        <!-- Close Button -->
        <ImageButton
            android:id="@+id/btn_close_place_card"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Close Place Card"
            android:src="@drawable/ic_close" />
    </LinearLayout>

    <!-- Action Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_12"
        android:orientation="horizontal">

        <Button
            android:id="@+id/btn_directions"
            style="@style/Typography.Button"
            android:layout_width="0dp"
            android:layout_height="44dp"
            android:layout_weight="1"
            android:background="@drawable/bg_button_primary"
            android:text="Get Directions"
            android:textColor="@color/white" />

    </LinearLayout>

</LinearLayout>
