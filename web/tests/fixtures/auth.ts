import { mockUser } from './users'

export const mockAuthTokens = {
  access_token: 'mock-jwt-access-token-xyz123',
  refresh_token: 'mock-jwt-refresh-token-abc789',
  token_type: 'bearer',
  user: mockUser,
}
